export enum RepeatMode {
    NONE,
    ONE,
    BOTH,
    ALL,
}

export enum SweaterPartArea {
    LeftArm,
    RightArm,
    FrontTorso,
    BackTorso
}
export enum SweaterPartAreaGroup {
    Arm,
    Torso
}